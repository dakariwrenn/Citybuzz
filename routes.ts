// Node.js/Express-style route skeletons

// POST /signup
// POST /login

// GET /cities               // List available cities
// POST /select-city         // Set user's city

// GET /feed?city=New York   // Get posts for a city

// POST /post
//   body: { city, content, imageUrl? }

// POST /reply
//   body: { postId, content }

// POST /upvote
//   body: { postId }

// POST /downvote
//   body: { postId }

// POST /report
//   body: { postId or replyId, reason }
